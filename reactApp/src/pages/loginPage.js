import React from "react";
import LoginWindow from "../components/loginWindow";

const LoginPage= ({history}) =>{
    return(
        <LoginWindow history={history}/>
    )
}
export default LoginPage;